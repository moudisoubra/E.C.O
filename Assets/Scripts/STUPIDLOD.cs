using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class STUPIDLOD : MonoBehaviour
{

    public List<LODGroup> LODOBJECTS = new List<LODGroup>();
    // Start is called before the first frame update
    private void Awake()
    {


        //LODOBJECTS.Add(FindSceneObjectsOfType(LODGroup));

    }
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
